services:
  - type: web
    name: acscraper
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn src.api.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000